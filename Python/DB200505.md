# TIL 2020-05-05

## 오늘 배운 내용

---

- DB를 SELECT로 검색하고 WHERE 문을 통해 원하는 조건을 통해 검색 하는 법을 공부했다.

- DB를 UPDATE SET을 통해 데이터를 수정하는 방법을 알았고,
똑같이 WHERE 조건으로 조건을 통해 수정하는 방법을 알았다.

- DB를 DELETE를 통해 테이블 내 데이터를 삭제하는 방법을 알았다.

- sqlite3에서 제공하는 iterdump() 메서드를 통해 DB의 내용을 반복문으로 sql   파일로 써서 데이터를 백업하는 방법을 알아보았다.

---